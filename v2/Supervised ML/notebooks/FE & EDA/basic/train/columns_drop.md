### About the columns dropped in the dataset (df_train)
- While applying the featured engineering, in the dataset, I dropped th columns in different versions like drop_cols_v1, drop_cols_v2 etc., so here's a brief guide to it.

### drop_cols_v1

- columns dropped : ['Addtional_Info', 'Airline']

### drop_cols_v2

- columns dropped : ['Date_of_Journey', 'Date_of_Journey_year']

### drop_cols_v3 

- columns dropped : ['Route', 'Dep_Time']

### drop_cols_v4 

- columns dropped : ['Arrival_Time']

### drop_cols_v5

- columns dropped : ['Duration', 'Duration_in_minutes']
